# CI Test Caddy Configuration
:80 {
    # API routes to backend
    handle_path /api/* {
        reverse_proxy auth-test-backend:8000
    }
    
    # Everything else to frontend
    reverse_proxy auth-test-frontend:3000
}