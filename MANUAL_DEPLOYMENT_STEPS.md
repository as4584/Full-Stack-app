#!/bin/bash
# Manual deployment instructions - Run these commands on the production server

echo "🚀 Manual Frontend Deployment Guide"
echo "===================================="
echo ""
echo "1. Update Caddy Configuration:"
echo "   sudo cp /opt/ai-receptionist/Caddyfile.production /etc/caddy/Caddyfile"
echo "   sudo systemctl reload caddy"
echo ""
echo "2. Deploy Dashboard Frontend:"
echo "   cd /opt/ai-receptionist/frontend"
echo "   docker compose -f docker-compose.prod.yml down"
echo "   docker compose -f docker-compose.prod.yml build --no-cache"
echo "   docker compose -f docker-compose.prod.yml up -d"
echo ""
echo "3. Deploy Auth Frontend:"
echo "   cd /opt/ai-receptionist/auth-frontend"
echo "   docker compose -f docker-compose.prod.yml down"
echo "   docker compose -f docker-compose.prod.yml build --no-cache"
echo "   docker compose -f docker-compose.prod.yml up -d"
echo ""
echo "4. Verify Deployment:"
echo "   curl -I https://lexmakesit.com/"
echo "   curl -I https://auth.lexmakesit.com/signin"
echo ""
echo "5. Check Container Status:"
echo "   docker ps | grep -E 'dashboard|auth'"
echo ""
echo "Expected Results:"
echo "  - lexmakesit.com should return HTTP 200 or 307"
echo "  - auth.lexmakesit.com should return HTTP 200"
echo "  - Both containers should show 'Up' status"
